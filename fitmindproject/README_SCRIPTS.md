# 🚀 FitMind iOS Launch Scripts

## Usage

### 1. First Time or When Simulator is Closed
```bash
./run_ios.sh
```

This script:
- ✅ Opens iOS Simulator
- ✅ Boots iPhone 16 Pro simulator
- ✅ Updates Flutter dependencies
- ✅ Builds and runs the application

### 2. When Simulator is Already Open (Quick Start)
```bash
./run_ios_quick.sh
```

This script:
- ✅ Only builds and runs the application
- ✅ Doesn't try to open the simulator (faster)

